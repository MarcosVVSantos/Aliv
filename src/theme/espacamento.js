// Espaçamento base de 8px — use sempre múltiplos (ver CONTEXTO.md, seção 5).

export const espacamento = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
};

// Constantes de layout que aparecem em várias telas.
export const raioCard = 16;
export const alvoToqueMinimo = 48; // dp — nenhum alvo de toque abaixo disso

export default espacamento;
