// Substitui src/services/config.js no seed: Firestore real (USAR_MOCK = false).
export const USAR_MOCK = false;
export const ATRASO_MOCK_MS = 0;

export function aguardar() {
  return Promise.resolve();
}
